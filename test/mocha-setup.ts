/**
 * Mocha setup file to mock VS Code dependencies before tests run.
 * This allows pure-function tests to run without requiring the full VS Code runtime.
 * Disabled in favor of register-mock-vscode.js approach.
 */

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const setupComplete = true;
