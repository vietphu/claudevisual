# Screenshots

`sidebar-collapsed.png` and `sidebar-expanded.png` are linked from the main
[README.md](../../README.md) "Screenshots" section. Replace one in place (same filename) if
you want to update the shot.

This folder is excluded from the packaged extension (see `docs/**` in
[.vscodeignore](../../.vscodeignore)) — it's GitHub-README-only and never ships in the `.vsix`.
