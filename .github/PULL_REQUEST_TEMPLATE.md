## What does this change

## Why

## Checklist
- [ ] `npm run typecheck`, `npm run compile`, and `npm test` pass locally
- [ ] If this touches the settings.json safe-write path (`src/hooks/json-merge.ts`,
      `src/hooks/installer.ts`, `src/config/config-writer.ts`,
      `src/hooks/hook-scripts/statusline-wrap.cjs`), I walked the relevant section of
      [docs/manual-live-checklist.md](../docs/manual-live-checklist.md)
- [ ] CHANGELOG.md updated under `[Unreleased]`
